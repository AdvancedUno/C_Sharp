<UserControl x:Class="Frism_Inspection_Renew.Components.SideBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frism_Inspection_Renew.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
            


        <!--<TextBlock Grid.Column="0" Text="aaaa"/>-->

        
        
        <Button Grid.Row="0" 
                Content="Home" 
                Padding="10"
            
                Command="{Binding NavigateHome}"/>

        <Button Grid.Row="1" 
                Content="SetCam" 
                Padding="10"
                Command="{Binding NavigateSetCamera}"/>

        <Button Grid.Row="2" 
                Content="SetDnn" 
                Padding="10"
                Command="{Binding NavigateSetDnn}"/>


    </Grid>
</UserControl>
